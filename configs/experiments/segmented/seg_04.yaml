parent_cfg: configs/experiments/segmented/seg_base.yaml

# Segment 4 of 13 of the T4 segmented-BG-merge pipeline.
# Frames [160, 220] (length 61).
# Stride 40 from previous segment (1/3 overlap) — except the last,
# which is shifted to cover the dataset tail (frame 560).

exp_name: t4_seg_04_f160_220
data:
  selected_frames: [160, 220]
